{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {{- if .Values.crd.keep }}
    helm.sh/resource-policy: keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.20.1
  name: dataprepperdefaults.dataprepper.kaasops.io
spec:
  group: dataprepper.kaasops.io
  names:
    kind: DataPrepperDefaults
    listKind: DataPrepperDefaultsList
    plural: dataprepperdefaults
    shortNames:
    - dpd
    singular: dataprepperdefaults
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataPrepperDefaults is the Schema for the dataprepperdefaults
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataPrepperDefaultsSpec defines shared defaults for pipelines
              in a namespace.
            properties:
              dataPrepperConfig:
                description: DataPrepperConfigSpec configures data-prepper-config.yaml
                  settings.
                properties:
                  circuitBreakers:
                    description: CircuitBreakerSpec configures Data Prepper circuit
                      breakers.
                    properties:
                      heap:
                        description: HeapCircuitBreakerSpec configures the heap circuit
                          breaker threshold.
                        properties:
                          usage:
                            type: string
                        required:
                        - usage
                        type: object
                    type: object
                  raw:
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              image:
                type: string
              kafka:
                description: DefaultKafkaSpec holds default Kafka connection settings.
                properties:
                  bootstrapServers:
                    items:
                      type: string
                    type: array
                  credentialsSecretRef:
                    description: SecretReference is a reference to a Kubernetes Secret
                      by name and optional key.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              resources:
                description: PerReplicaResources wraps resource requirements applied
                  per replica.
                properties:
                  perReplica:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: ResourceList is a set of (resource name, quantity)
                          pairs.
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: ResourceList is a set of (resource name, quantity)
                          pairs.
                        type: object
                    type: object
                type: object
              serviceMonitor:
                description: ServiceMonitorSpec configures Prometheus ServiceMonitor
                  creation.
                properties:
                  enabled:
                    type: boolean
                  interval:
                    type: string
                type: object
              sink:
                description: DefaultSinkSpec holds default sink settings.
                properties:
                  opensearch:
                    description: OpenSearchSinkSpec configures an OpenSearch sink.
                    properties:
                      credentialsSecretRef:
                        description: SecretReference is a reference to a Kubernetes
                          Secret by name and optional key.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      dlqSecretRef:
                        description: SecretReference is a reference to a Kubernetes
                          Secret by name and optional key.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      hosts:
                        items:
                          type: string
                        minItems: 1
                        type: array
                      index:
                        type: string
                      indexType:
                        type: string
                      routes:
                        items:
                          type: string
                        type: array
                    required:
                    - hosts
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true

{{- end }}
